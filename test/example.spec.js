// Create a group of tests for the script file
describe("a suite of tests", () => {
  // Create a group of tests for a specific functionality
  describe("an example passing test", () => {
    it("is always true", () => {
      true === true;
    });
  });
});
